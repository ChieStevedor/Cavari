export default function ExperimentsPage() {
  return (
    <div className="flex flex-col gap-2">
      <h1 className="text-lg font-semibold tracking-tight">Experiments</h1>
      <p className="max-w-md text-sm text-muted-foreground">
        Post-launch growth experiments (§21) come in a later phase, per the
        spec&apos;s phased build order — Phase 1 covers pre-launch validation
        experiments instead, under Validation.
      </p>
    </div>
  );
}
